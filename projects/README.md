# Папка projects

Здесь лежат мои мини-проекты, которые я делаю параллельно с изучением Python и автоматизации.

---

## 🕹 lesson12.py — игра «Космический защитник»

Аркадная мини-стрелялка на Pygame.

**Что делает:**
- открывает окно 800x600 с космическим фоном;
- создаёт корабль игрока, астероиды и пули;
- игрок двигается влево/вправо (`←` и `→`);
- стреляет по пробелу;
- пули уничтожают астероиды, начисляются очки;
- при столкновении астероида с кораблём уменьшается здоровье;
- при потере здоровья до 0 игра заканчивается, показывается экран Game Over;
- есть стартовый экран и перезапуск игры.

**Технологии и темы:**
- `pygame`;
- спрайты (`Sprite`, `Group`);
- обработка событий клавиатуры;
- столкновения спрайтов (`groupcollide`, `spritecollide`);
- циклы игры (game loop).

---

## 🌐 lesson13_stealth.py — стелс-автотест Google (Selenium)

Сценарий автоматизации браузера, который пытается вести себя как «живой» пользователь.

**Что делает:**
- запускает Chrome с настройками для уменьшения палевности Selenium;
- открывает сайт `https://www.google.com`;
- находит поле поиска разными способами (name, class, css, xpath);
- вводит запрос с задержками между символами, имитируя набор текста;
- отправляет поиск;
- ждёт результаты и пытается найти блоки выдачи по нескольким селекторам;
- делает скриншоты страницы и сохраняет HTML при проблемах;
- проверяет, не появилась ли CAPTCHA.

**Технологии и темы:**
- `selenium` + `webdriver_manager`;
- `ChromeOptions`, user-agent, отключение `enable-automation`;
- ожидания (`WebDriverWait`, `expected_conditions`);
- работа с CSS-селектрами и XPATH;
- обработка ошибок, логирование.

---
